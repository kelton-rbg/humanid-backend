from flask import Flask, request, jsonify, redirect from flask_cors import CORS import requests import jwt import datetime import os import stripe  app = Flask(__name__) CORS(app)  app.config['SECRET_KEY'] = 'supersecretkey'  # Stripe stripe.api_key = os.getenv("STRIPE_SECRET_KEY")  # Fake database users = {} user_credits = {}  # Stripe price IDs PRICE_IDS = {     "starter": "price_1TVoTBRyx4mChIbOb3R037Mi",     "basic": "price_1TVoUpRyx4mChIbOuJvWg0YO",     "pro": "price_1TVoXJRyx4mChIbOeH9xeRAO",     "ultimate": "price_1TVoZ7Ryx4mChIbO3Z30mUQJ" }  # Credit packs CREDITS = {     "starter": 20,     "basic": 50,     "pro": 150,     "ultimate": 500 }  # Redirect WWW + HTTPS @app.before_request def force_domain_redirect():     host = request.host     url = request.url      if host.startswith("www."):         return redirect(url.replace("www.", ""), code=301)      if request.headers.get("X-Forwarded-Proto") == "http":         return redirect(url.replace("http://", "https://"), code=301)  # Home @app.route("/") def home():     return "HumanID backend running 🚀"  # Register @app.route("/register", methods=["POST"]) def register():      data = request.json      email = data.get("email")     password = data.get("password")      if email in users:         return jsonify({"error": "User exists"}), 400      users[email] = {         "password": password,         "credits": 150     }      return jsonify({         "message": "User created"     })  # Login @app.route("/login", methods=["POST"]) def login():      data = request.json      email = data.get("email")     password = data.get("password")      user = users.get(email)      if not user or user["password"] != password:         return jsonify({             "error": "Invalid credentials"         }), 401      token = jwt.encode({         "email": email,         "exp": datetime.datetime.utcnow() + datetime.timedelta(days=1)     }, app.config['SECRET_KEY'], algorithm="HS256")      return jsonify({         "token": token     })  # Verify token def verify_token():      token = request.headers.get("Authorization")      if not token:         return None      try:         data = jwt.decode(             token,             app.config['SECRET_KEY'],             algorithms=["HS256"]         )          return data["email"]      except:         return None  # Get credits @app.route("/credits", methods=["GET"]) def credits():      email = verify_token()      if not email:         return jsonify({             "error": "Unauthorized"         }), 401      return jsonify({         "credits": users[email]["credits"]     })  # Analyze @app.route("/analyze", methods=["POST"]) def analyze():      email = verify_token()      if not email:         return jsonify({             "error": "Unauthorized"         }), 401      if users[email]["credits"] <= 0:         return jsonify({             "error": "No credits"         }), 403      data = request.json     text = data.get("text", "")      users[email]["credits"] -= 1      try:          response = requests.post(             "https://api.sightengine.com/1.0/text/check.json",             data={                 "text": text,                 "lang": "en",                 "mode": "standard",                 "api_user": "TEU_API_USER",                 "api_secret": "TEU_API_SECRET"             },             timeout=3         )          result = response.json()          return jsonify({             "result": result,             "credits_left": users[email]["credits"]         })      except:          return jsonify({             "risk": "medium",             "message": "Fallback mode",             "credits_left": users[email]["credits"]         })  # Stripe Checkout @app.route("/create-checkout-session", methods=["POST"]) def create_checkout_session():      data = request.json      pack = data.get("pack")     user_id = data.get("user_id")      if pack not in PRICE_IDS:         return jsonify({             "error": "Invalid pack"         }), 400      session = stripe.checkout.Session.create(         payment_method_types=["card"],         mode="payment",         line_items=[{             "price": PRICE_IDS[pack],             "quantity": 1         }],         metadata={             "user_id": user_id,             "pack": pack         },         success_url="https://humanid.online/success",         cancel_url="https://humanid.online/cancel"     )      return jsonify({         "checkout_url": session.url     })  # Stripe webhook @app.route("/stripe-webhook", methods=["POST"]) def stripe_webhook():      payload = request.data     sig_header = request.headers.get("Stripe-Signature")      endpoint_secret = os.getenv("STRIPE_WEBHOOK_SECRET")      try:          event = stripe.Webhook.construct_event(             payload,             sig_header,             endpoint_secret         )      except Exception as e:          return jsonify({             "error": str(e)         }), 400      if event["type"] == "checkout.session.completed":          session = event["data"]["object"]          user_id = session["metadata"]["user_id"]         pack = session["metadata"]["pack"]          credits_to_add = CREDITS.get(pack, 0)          current = user_credits.get(user_id, 0)          user_credits[user_id] = current + credits_to_add          print(f"Added {credits_to_add} credits to {user_id}")      return jsonify({         "status": "success"     })  if __name__ == "__main__":     app.run(debug=True)
